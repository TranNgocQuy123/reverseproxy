apiVersion: apps/v1
kind: Deployment
metadata:
  name: udagram-reserveproxy
  labels:
    run: udagram-reserveproxy
spec:
  replicas: 2
  selector:
    matchLabels:
      run: udagram-reserveproxy
  template:
    metadata:
      labels:
        run: udagram-reserveproxy
    spec:
      containers:
      - name: udagram-reserveproxy
        image: hiennv00/udagram-reserveproxy:lasted
        ports:
        - containerPort: 80
        resources:
          limits:
            cpu: 500m
          requests:
            cpu: 200m